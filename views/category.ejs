<div class="mb-4">
  <a href="/" class="text-decoration-none text-muted small">&larr; Voltar para todas as categorias</a>
</div>

<div class="text-center mb-4">
  <h1 class="display-6 text-gold fw-bold"><%= category.name %></h1>
  <p class="text-muted">
    Produtos selecionados em <%= category.name.toLowerCase() %>.
  </p>
</div>

<div class="row g-4">
  <% if (!products || products.length === 0) { %>
    <div class="col-12 text-center text-muted">
      Nenhum produto nesta categoria ainda.
    </div>
  <% } %>
  <% products.forEach(function (product) { %>
    <div class="col-6 col-md-4 col-lg-3">
      <div class="card product-card h-100 bg-black border-gold">
        <% if (product.image_url) { %>
          <img
            src="<%= product.image_url %>"
            class="card-img-top product-img"
            alt="<%= product.name %>"
          />
        <% } else { %>
          <div class="product-placeholder d-flex align-items-center justify-content-center">
            <span class="text-muted small">Sem imagem</span>
          </div>
        <% } %>
        <div class="card-body d-flex flex-column">
          <h5 class="card-title text-gold"><%= product.name %></h5>
          <% if (product.description) { %>
            <p class="card-text small flex-grow-1">
              <%= product.description %>
            </p>
          <% } %>
          <% if (product.price != null) { %>
            <div class="fw-bold text-gold mb-2">
              R$ <%= Number(product.price).toFixed(2).replace('.', ',') %>
            </div>
          <% } %>
          <a
            href="https://wa.me/<%= whatsappNumber %>?text=<%= encodeURIComponent('Olá, tenho interesse no produto: ' + product.name) %>"
            target="_blank"
            rel="noreferrer"
            class="btn btn-gold btn-sm mt-auto"
          >
            Falar no WhatsApp
          </a>
        </div>
      </div>
    </div>
  <% }) %>
</div>


